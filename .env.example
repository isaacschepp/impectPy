# Copy this file to .env and fill your values
# The script will load .env from the repo root if python-dotenv is installed.

IMPECT_USERNAME=your_email@example.com
IMPECT_PASSWORD=your_password_here
# Or use a token instead of username/password
# IMPECT_TOKEN=...

# Optional: select a specific Windows Credential Manager target
# IMPECT_CRED_TARGET=Your-Credential-Target

# Optional: list discovered Windows credential targets on startup
# IMPECT_LIST_CREDENTIALS=1

# Default iteration (script default is 1236)
# IMPECT_ITERATION=1236
